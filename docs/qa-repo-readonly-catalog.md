# QA 仓场景清单（只读）

> 平台读别人家的黑盒验收仓，把「场景清单 + 每个场景规划了哪些脚本」画成一页。
> **对该仓库永远只读**：只做 `clone --bare` / `fetch` / `git show` / `git grep`。

页面：项目内「测试设计 → QA 对账」（`/projects/:projectId/qa-catalog`）
配置：**就在这一页**，右上角「配置 QA 仓」。只填仓库地址，其余自动识别。

---

## 1. 为什么是只读，而且这条不能松

QA 仓（如 uag-qa）是**别人维护的仓库**，里面只有验收脚本，没有产品代码。清单文件是那个
仓库的**唯一索引**，也是 QA 自己的工作面：`check-coverage.sh` 会拿"清单标 ✅ 但没有脚本
声明这个 ID"当 BLOCK 拦下来。平台一旦往里写（补 ID、改状态列、塞 metadata 文件），
就等于替对方改了他的判据来源 —— 他的门禁会开始红，而红的原因在他仓库里查不到。

所以这条是硬约束，不是"当前还没做写回"：

- `app/services/qa_catalog.py` 里只允许出现 `show` / `ls-tree` / `rev-parse` / `log` / `grep` / `symbolic-ref` 这些读子命令；
- `backend/tests/test_qa_catalog_parse.py` 有两条封样，扫模块源码，出现 `push`/`commit`/`worktree`/
  `checkout` 之类的写子命令直接红；同一份文件里还有一组真 git 仓的解析/自动识别测试；
- `tests/api/projects/test_qa_catalog_config.py` 拿一个本地临时 git 仓当 QA 仓，把
  「保存配置 → 立刻按新配置读一次」整条跑通（本地路径 clone 不用网络，所以能进常规 CI）；
- 也**不要求对方仓库为我们改任何东西**：不加字段、不加文件、不加 CI 钩子。
  我们读的全是他本来就在维护的东西（清单表格 + 文件头注释）。

想让平台的用例流回 QA 仓？那是**另一件事**，得先跟仓库主人谈，不能从这个模块长出来。

## 2. 读什么

| 读 | 是什么 | 用途 |
|---|---|---|
| 清单文件（自动认出来，见 §3） | 场景的**分母**：该有哪些场景、P/R/层、状态 ✅⬜❌ | 覆盖率、缺口、优先级分布 |
| 用例脚本头部注释 | 场景的**分子**：`@scenario`（可多个）/ `@tier` / `@known-bug` | 每个场景挂了哪些脚本、哪些带着已知缺陷 |

两边**都读、都如实呈现**，不做"以清单为准"的抹平 —— 因为两边对不上才是最该看见的信息：

- **清单未对上**（`claimedButUncovered`）：状态标 ✅ 但没有任何脚本声明这个 ID。
- **孤儿脚本**（`orphanScripts`）：脚本声明了清单里不存在的 ID。

这两条正是 QA 自己 `check-coverage.sh` 会**直接 BLOCK** 的两项，所以页面把它们放在第三块
看板「清单可信吗」里，而不是藏在表格里的一个小黄标 —— 见 §5。

清单行长这样（列少了不会整份读不出来，按缺省补空）：

```
| ID | 场景 | P | R | 层 | 状 |
| SMK-01 | `GET /healthz` 返回 200 | P0 | 6 | smoke | ✅ |
| MCP-38 | HITL 挂起的调用事件也要带三段式操作人 | P1 | 6 | scenario | ✅ `@known-bug GL#531` |
```

脚本头长这样（`#` 和 `//` 两种注释都认，只扫前 25 行）：

```bash
# @scenario AUT-01 AUT-02 AUT-05     ← 第一个是主 ID，决定文件放哪个域目录
# @tier smoke
# @known-bug GL#531 审计记录各缺一半
```

域码不写死（别的项目有别的域码）：只要求「2–6 位大写字母 + 短横 + 2–3 位数字」，
域名从清单里的域码表读。`@scenario` 那行只取形如 ID 的 token，遇到第一个不像 ID 的就停 ——
行尾的中文说明（`← 第一个是主 ID`）否则会被当成场景 ID，在页面上变成一串假的孤儿脚本。

**模板文件不算用例**：`*.tmpl` / `*.example` 这类，或路径里有 `templates/` 的，
自动发现时跳过。uag-qa 的 `.claude/skills/qa-module/templates/case.sh.tmpl` 里写着占位的
`@scenario XXX-01`，算进来页面上就会多一条"声明了清单外 ID 的脚本"，看着像 QA 写错了。
真要把它当用例看，去高级里填 glob —— 填了就完全按 glob 来，不再自动排除。

## 3. 配置项

存在 `projects.qa_repo`（JSONB，迁移 `zzt0qarepo`）。**只有仓库地址是必填的**，
另外三项是「认错了才需要填」的覆盖项，弹窗里收在「高级」里：

| 字段 | 留空时 | 说明 |
|---|---|---|
| `url` | — | 唯一必填。**清空 url 并保存 = 取消配置**（服务端把整个 `qa_repo` 置 NULL） |
| `branch` | 跟仓库自己的默认分支（bare 仓 HEAD 指的那个**名字**，取的是 `origin/<它>`，见下方那个坑） | 填了就必须命中，找不到直接报错 —— 悄悄回退 HEAD 等于拿别的分支的数据顶着你填的分支名显示 |
| `catalogPath` | 找**场景行最多**的那份 `.md`（≥5 行才算） | 不认文件名：别的 QA 仓不会也叫 `test-scenario-catalog.md`。README 里举两行例子不会被选中 |
| `caseGlobs` | `git grep -l @scenario` 捞所有声明了场景的文件 | 比配目录 glob 准：脚本挪目录不用改配置，也不会把没声明场景的支持库当用例。填了就只认这些 glob（`**` 跨目录、`*` 不跨） |

**别给这三项塞默认值。** `docs/test-scenario-catalog.md`、`api/**/*.sh` 是 uag-qa 一家的习惯，
写成默认值会让下一个 QA 仓在页面上报"没有这个文件"，而人根本没填过它 —— 那种错最难查。
页面顶部会显示这次是「自动识别」还是「配置指定」，认错了才知道要去高级里手填。

**配置维护在 QA 页，不在「编辑项目」弹窗**：这份配置只影响这一页、认错了也只在这一页报错，
改的人和看结果的人是同一个。附带的好处是权限对得上 —— `PUT /api/projects/{id}` 要系统 admin，
而 `PUT /api/projects/{id}/qa-catalog/config` 只要项目管理员，接自己项目的 QA 仓不用求平台管理员。

没配置时页面**只画表头** + 顶部一条提示，不是 404、不是空白页 —— "没配"和"配错了"是两件事，
页面必须能分得开。

本地缓存：`backend/.qa-repos/<project_id>.git`（bare，已进 `.gitignore`）。
`GET` 只读本地缓存不打远端；点「拉取最新」或刚保存完配置才 `fetch`。同一个 commit 不重复解析。
缓存按项目建，**换了仓库地址会先把旧缓存整个删掉**（不删的话 `ensure_bare_repo` 见 HEAD 还在
就直接复用，页面上地址已经改了、数据还是旧那家的）。

### 又一个坑（更毒）：fetch 成功了，但页面读的还是 clone 那一刻

**症状**：点「拉取最新」，提示"已拉取"，数字一个都不动。查 `FETCH_HEAD` 的时间是刚刚，
`refs/remotes/origin/main` 也确实前进了 —— fetch 没问题，**是读的时候读错了 ref**。

`clone --bare` 会把 `refs/heads/*` 铺出来并让 HEAD 指向默认分支；而之后每次 fetch 的
refspec 是 `+refs/heads/*:refs/remotes/origin/*`（`ensure_bare_repo` 写死的，另一个消费方
`sync_branch_first_time` 靠它），**只写 remotes，永远碰不到 `refs/heads/*`**。
于是 `_resolve_ref` 里「分支留空 → 读 HEAD」这条路拿到的永远是第一次 clone 的快照。

**为什么这么久没发现**：填了分支名的那条路本来就先试 `refs/remotes/origin/<branch>`，是对的；
只有留空那条错 —— 而"留空"正是上面这张表推荐的用法。**推荐路径踩坑，替代路径没事**，
所以谁去填一下分支名就"好了"，看起来像玄学。

**实测代价**（uag-qa，2026-08-26）：`refs/heads/main` = 263594daf（8/25）落后
`refs/remotes/origin/main` = 2189f8662（8/26）**16 个提交**，页面差出：

| | 旧（clone 快照） | 真实 | 差 |
|---|---|---|---|
| 覆盖率 | 47% | 53% | 少算 6 个点 |
| 已覆盖 / 待补 | 172 / 197 | 193 / 171 | 缺口**多报 26 条** |
| 脚本数 | 90 | 106 | 少 16 个 |
| 挂已知缺陷的场景 | 9 | 12 | 少 3 |

**修法**：两条路都走 `_pick_ref()` —— `refs/remotes/origin/<b>` 优先，`refs/heads/<b>` 兜底
（第一次 clone 之后 `sync_and_read` 会跳过 fetch，那一趟 remotes 还是空的），最后才 HEAD（游离头）。
封样在 `test_empty_branch_sees_new_commits_after_fetch`：**必须用「上游动了 + fetch 一次」来验**，
只断 ref 字符串或只跑一次 clone 都看不出这个 bug。

### 「拉取于」以前打的是提交时间

同一次排查里发现的第二个：页脚那行「拉取于 …」取的是 `commitDate`（`git log -1 --format=%cI`），
也就是**提交时间冒充拉取时间**。后果是这行字**恰好在最需要它的时候骗人**——QA 那边几天不提交，
它就一直显示同一个时间，而"这页多久没拉过"永远看不出来；反过来 fetch 到一个旧提交时它还会往后跳。

现在 `repo_meta` 里两个都给：`commitDate`（提交于）和 `fetchedAt`（拉取于），页脚显示后者
并带「N 小时前」，超 6 小时标黄。`fetchedAt` **不新存状态** —— `FETCH_HEAD` 是 git 每次
fetch 都重写的文件（哪怕这次没有新提交），它的 mtime 就是答案；还没 fetch 过退回 `HEAD` 的
mtime（= clone 时间）。**别用 config 的 mtime**：`ensure_bare_repo` 每次都写一遍 config，
那个时间永远是"刚刚"。

### 一个踩过的坑：`git grep` 的 ref 位置

```
git grep -c -E '<pat>' -- '*.md' HEAD    # ← HEAD 落在 pathspec 那侧，被当文件名找 → 0 命中
git grep -c -E '<pat>' HEAD -- '*.md'    # ← 对
```

写错了不会报错，只会"一条都没搜到"，表现成"这个仓库里没有场景清单"。
`_grep()` 现在把 ref 和 pathspec 分成两个参数，测试里用真仓库跑，锁住这个顺序。

## 4. 实测（UAG 项目，2026-08-26，commit dae9b4fc4）

357 行清单 / 24 个域 / 84 个用例脚本（清单路径和脚本范围都是自动识别出来的，配置里只填了仓库地址）：

- 分母 353（不含 4 条已废弃）：已覆盖 166、待补缺口 187
- P0 101/182（55%）、P1 60/145（41%）、P2 5/26（19%）
- **13 个「已覆盖」场景挂着 `@known-bug`**（GL#530/531/532/534）—— 13 条全都在已覆盖那边，
  也就是 47% 这个数字里含着 13 条**已知是红的**
- P0 且待补且风险 9 的：**59 条**（页面上一键能筛出来的那批）
- 清单未对上 0，孤儿脚本 0 —— 和 QA 自己 `check-coverage.sh` 的结论一致
- 风险 ≥6 却排在 P2/P3 的：1 条（AUT-16）

## 5. 页面按「测试员下一步干什么」排的

第一版把 total/covered/gap/deprecated/scripts 六个数平铺成六张卡片，问题不是不好看，是**看不懂**：
`P` `R` `层` `状` 四个单字列名是 QA 仓内部的缩写，平台这边没有任何地方解释 `R` 是什么；
一个数字看完之后没有下一步动作。改版后的规矩是两条：

1. **每个数字都是一个筛选入口** —— 点覆盖率里的 P0 行就筛 P0，点「P0 待补 · 风险 9 —— 59 条」
   就筛出那 59 条并按风险倒序，点域名就筛那个域。
2. **口径全部照抄 QA 自己的定义**，不自造词。`R = 概率(1–3) × 影响(1–3)`、
   「P 和 R 是两条独立的轴，不许互相推导」都出自那个仓库的 `docs/test-scenario-catalog.md` §1.1。

三块看板：

| 看板 | 回答的问题 | 关键点 |
|---|---|---|
| 覆盖到哪了 | 覆盖率 + 分优先级进度 | 明说分母不含已废弃；红字点破「其中 N 条挂着已知缺陷」 |
| 还欠多少 | 缺口总数 + 分优先级 | 一个红按钮直达「P0 待补 · 风险 9」 |
| 清单可信吗 | 清单和脚本对得上吗 | 前两项是 `check-coverage.sh` 会 BLOCK 的；第三项（风险 ≥6 却排 P2/P3）只是提醒 |

**「已覆盖」只代表有脚本声明了这个场景 ID，不代表这条跑过、更不代表跑绿了。** 平台只读仓库，
拿不到运行结果。这句话写在页面的「怎么读这一页」图例里 —— 不写的话 47% 会被当成"47% 是绿的"。

对应的服务端字段（都在 `_assemble` 里算，前端不再自己 filter 一遍）：

| 字段 | 含义 |
|---|---|
| `summary.coveredWithBugs` | 已覆盖但脚本头挂着 `@known-bug` 的场景数（**覆盖率虚高多少**） |
| `summary.knownBugScenarios` | 所有带已知缺陷的场景数（含待补的） |
| `summary.riskMismatch` | 风险 ≥6（`HIGH_RISK`）却排在 P2/P3 的场景数 |
| `summary.byPriority` | `{P0: {total, covered, gap}, ...}`（不含已废弃） |
| `domains[].gap` / `domains[].p0Gap` | 按域找「黑洞域」用；页面按 `gap` 倒序排，`p0Gap` 决定先啃哪个 |

## 6. 顺带清掉的三个入口

编辑项目弹窗现在只剩「项目名称 + 描述」。去掉的是「Git 仓库地址」「脚本基础路径」，
外加 QA 仓那一组字段（挪到 QA 页，见 §3）。前两个的理由：

- `git_url` 的唯一消费者是 `POST /projects/:pid/branches/:bid/sync`，而前端触发它的
  「同步用例」按钮早就没了；
- `script_base_path` 只有 `script_ref_file` 那种老用例执行时要（沙箱 checkout），
  这类用例只存在于「测试平台」项目（323/337），之后新建的项目一条都没有。

**数据库列、schema、执行引擎里的代码都留着** —— 老用例还得跑。去掉的只是"新项目还能在页面上
填这两个字段"这条路。真要给老项目改这两个值，改库或走接口。

## 7. 打开脚本正文看（`GET /file`）

清单只告诉你「AUT-01 由 `api/authz.sh` 覆盖」。要判断这份脚本到底验没验到 AUT-01
描述的那件事，得看正文 —— 所以场景行里的脚本名、以及「有脚本没进清单」那一列的路径，
都能点开。

**能读哪些文件是白名单，不是路径校验。** `qa_catalog.readable_paths()` 只放行两类：
清单文件本身、以及**清单已经引用到的脚本**（含 orphan 扫描扫到的那批）。请求的路径不在
这个集合里，直接 404 `QA_FILE_UNREADABLE`，连 `git show` 都不发。

为什么不做"路径归一化 + 前缀检查"：QA 仓里躺着 `secrets.env`、`.gitlab-ci.yml`
这类东西，我们没有任何理由把它们代理出来。白名单是"只给出我们本来就要展示的"，
`../../etc/passwd`、`docs/../secrets.env` 这类写法不需要单独处理就已经不在集合里。
（封样测试：`tests/api/projects/test_qa_catalog_file_and_review.py::test_清单没引用的文件读不到`，
它往仓库里塞了一份带假 token 的 `secrets.env`，断言响应体里搜不到那串 token。）

## 8. 域级 AI 评审（`POST /reviews`）

### 它答的是门禁答不了的那个问题

`check-coverage.sh` 能拦三件事：声明了清单外的 ID、标 ✅ 却没有脚本声明、目录跟主域码
对不上。三条都是**对账**，grep 就能做。它拦不住的是里子：**脚本声明了
`@scenario AGT-11`，它到底有没有验 AGT-11 说的那件事。** 一个 `curl` 一下拿到 200 就
`exit 0` 的脚本，在门禁那边是"已覆盖"。这是烧一次模型调用的唯一理由。

**按域评，不按条评。** 用例审核那边逐条评是因为结论要写回 `cases.review_status` 管门禁；
这里没有门禁、也没有可写的行。域是 QA 仓自己的组织单位（脚本按域码放目录），
「AGT 这 11 条场景撑不撑得住」本来就是他们排活的粒度。

### 只读怎么保证

全程只有 `git show`。结论落在**本库** `qa_catalog_reviews`，QA 仓一个字节不变。
prompt 里还明写了「不许建议我们去改这个仓库的流程、加字段、加钩子」——
模型很容易顺口给出"建议在脚本头加 `@asserts` 字段"这类建议，那是别人的仓库。

### 环境是结论的一部分，而且这一块不问模型

选环境不是走过场：缺 `ADMIN_TOKEN` 的环境上，这个域一条都跑不起来，这跟脚本写得好不好
是两件独立的事。`env_gaps()` 用**纯代码**算，变量名能精确算，让模型猜只会又慢又飘。

**只传变量名，绝不传值。** 环境变量里放的是真凭证。

这一列 2026-08-26 在 uag-qa 的 MCP 域上返工过三轮，五个坑都是实测撞出来的：

| 坑 | 后果 | 怎么处理 |
|---|---|---|
| 按行找赋值，只认每行第一个 | `export A=1 B=2`、`A=""; B=""` 里后面那些全成了"缺的"，14 份脚本 5 个假阳 | 按语句切（`;`/`&&`/`\|\|`/换行）再匹配 |
| `source` 只走一层 | 脚本 source 的是 `config/env.sh`，定义 TOKEN 的 `lib/auth.sh` 是它再 source 的；不跟下去这些全算缺 | 跟着 source 链走，上限 20 份 |
| 夹具运行时拼名字 | `printf -v "${1}_ID"` 造出 MB_ID/MB_TOKEN/…，仓库里搜不到 `MB_USER=` | 认**后缀**（`_ID`/`_TOKEN`/…）整族放过；花括号必须有，否则 `${QA_ROOT}` 会被拆成 `${QA}`+`_ROOT`，把 `_APIKEY`/`_DSN` 也变成"动态后缀"，真缺口一起吃掉 |
| `export X="${X:-}"` 被当成定义 | **反过来的漏报。** uag-qa 的 `config/env.sh` 拿它当外部输入清单；脚本那头是 `[ -n "${UAG_APIKEY:-}" ] \|\| skip_case`，带默认值按"引用"也扫不出来 —— 没配就整条静默跳过，报告是绿的，一条数据面用例都没跑 | 只认**空兜底**：`${X:-}` 算"要从外面传"，直接进缺口；`${X:-http://localhost:3000}` 自带兜底值，不算 |
| 上一条**带了尾注释就不认** | 同一类漏报，而且专挑最该报的那条：`export PASSWORD="${PASSWORD:-}"  # 刻意不给默认值,强制外部注入`。匹配失败不是"不知道"，是直接倒向"定义过了" —— 而 `PASSWORD` 一缺，`require_login` 就过不去，**整个域**都是 skip，不只是数据面 | 正则允许尾随 `# …`；带真兜底值的（`${WEB_URL:-http://127.0.0.1:3000}  # 前端`）照旧不算 |

改完的实测（MCP 域 / 环境 `uag-138:3000`）：18 份脚本、跟出 6 份公共库、
缺口从 12 个（5 个假）变成 **7 个，全部真缺、全部指向 `config/env.sh` 的声明** ——
`PASSWORD`、`UAG_APIKEY`、`PSQL_DSN`、`PSQL_PGPASSWORD`、`PSQL_DOCKER_CONTAINER`、
`UAG_INTERNAL_TOKEN`、`UAG_JWT_SECRET`。上一轮模型是靠通读 `lib/common.sh` 才发现
`UAG_APIKEY`/`PSQL_DSN` 的，现在纯代码就能给出来。

**一个必须写清楚的口径**：这一列比的是「QA 脚本要的变量名」和「平台这条环境记录着的变量名」，
两边的命名习惯不是一套 —— `uag-138:3000` 里有 `ADMIN_PASSWORD`，QA 那边叫 `PASSWORD`，
同一个东西两个名。所以它答的是「**照这个环境的记录，这套脚本跑起来缺什么**」，
不是「这个环境是坏的」。更要紧的是：**在平台这边补上变量并不会让 QA 的脚本真跑起来** ——
脚本在 QA 侧执行，值由他那边的 CI 变量注入。平台这边补，只让评审结论对得上现实；
现实要变，得在真正跑套件的地方注入。两件事都做了才算数，只做前一件就是把
「忘了配」粉刷成「配过了」，比不补更坏。

### 一次实跑长什么样

MCP 域、53 条场景、14 份脚本（当时的旧额度）、`claude-opus-5`，**5 分 13 秒**出结论 `risky`：
指出三条 P0/R9 的场景「脚本能绿但产品没修也照样绿」，并给了 6 条待补的先做顺序。
慢是正常的（一次要读几万字脚本正文），所以做成后台任务 + 列表页轮询，
同一个域已经在排队/在跑就不重复发。

### 一份结论要给两拨人看，所以分两页写

同一次评审的输出分成两块，页面上是两个页签（默认落在「给人看」那一页）：

| | 读它的人 | 内容 | 硬规矩 |
|---|---|---|---|
| `brief` | 测试经理 / 项目经理，三十秒决定"要不要停下来处理" | 一句话结论 + 至多 4 条要点 + 下一步 | **不许出现脚本路径、变量名、函数名、断言写法、状态码**；每一条都必须是细节里某一条的人话版；要带数字，"覆盖率偏低""建议加强"一律不许写 |
| `summary` / `scriptGaps` / `envMissing` / `catalogGaps` / `nextUp` | 动手整改的人，或 QA 那边的 Claude Code | 逐条：哪个脚本、哪一行、问题是什么、改成什么 | 每条带 `evidence` —— 从脚本正文**原样抄** ≤3 行当锚点，抄不出来就留空，**不许编** |

两条实现上的取舍，都是踩过才定的：

- **一次模型调用出两份，不是调两次。** 分两次调的话，人话那份是模型对自己那份结论的
  二次转述，会漂 —— 而漂了没人发现（读人话那页的人不会去核细节那页）。
  所以 `brief` 被约束成"细节的人话版"，同一次生成、同一个上下文。
- **模型不给 `brief` 就退回 `summary`，绝不留空**（`_brief()`）。留空的后果不是"少一块内容"：
  「给人看」那页整版空白，而人不会去点隔壁那页 —— 他只会得出"这个域没问题"。
  这个字段上线前评的老记录走的也是这条退路。

`evidence` 是给整改那一侧的锚点：拿它直接 `grep` 就能定位到脚本里那一行。
没有它，"断言不够"就只是一句评价 —— 接手的人还得自己把 18 份脚本重读一遍。

### 「谁动手」必须由结论自己带（`blame`）

第一版把三类问题堆在同一张表里，实测就翻车了：MCP 域抓到 6 条，其中 2 条的根子是
**我们这条环境记录里没有 `UAG_APIKEY`**，跟人家脚本一点关系没有。读的人第一眼
当成"人家脚本写得不行"，想了一会儿才反应过来是配置的事 —— **让人自己分栏，
他分完还得怀疑自己分对了没有**。

所以每条结论必须自报 `blame`，判据是"改脚本能不能解决"：

| `blame` | 意思 | 谁去改 |
|---|---|---|
| `script` | 断言写得站不住，跑绿证明不了它认领的那件事 | 仓库主人 —— **只有这一类才该发给他** |
| `env` | 脚本可能写得很对，只是在这个环境里自己 `skip` 了 | 铺环境的人 |
| `catalog` | 脚本和环境都没错，是清单认领的口径不对 | 两边一起商量 |

认不出来的落 `script`（宁可多审一条，也别把该发的漏进"不是你的事"那堆）。
页面上「QA 的脚本要改」那个数字只数 `script` 那一栏 —— 上一版数的是全部，
等于把我们自己的配置缺口记在人家账上。

**`envMissing` 那一列尤其要小心，它是最容易变成冤枉人的一列。** 它比的是
「脚本引用的变量名」和「**我们这侧**环境记录里存的变量名」：

- 平台这边补上变量 **不会** 让 QA 的脚本真跑起来 —— 值要在真正跑套件的地方注入；
- 平台这边没记着，也 **不等于** 那边缺 —— QA 自己 runner 里有什么，我们根本看不到。

第一版的 prompt 里写的是「判『能不能跑起来』时可以直接用」，模型就拿它当铁证，
把「我们没记着这个名字」写成了「场景层一条没跑、已覆盖是假的」。现在 prompt 明写：
由这一列得出的结论一律 `blame=env`，措辞只能到「在这个环境里会跳过」，
**不许**升级成「脚本没验到」。

### 结论词换过两轮才对：**别用一个词概括，直接把那句话写出来**

- 第一版「靠得住 / 有水分 / 撑不住」—— 三个都是比喻。"水"在哪？"撑"的是什么？
- 第二版「能信 / 信一半 / 不能信」—— 还是要猜。信什么？信一半是哪一半？

两版都在同一个坑里：**这一栏根本不是一个形容词能装下的东西。**
它答的是一个很具体的问题：*脚本头写了 `@scenario X`，它到底验没验 X？*
所以第三版不概括了，直接把那句话写出来：

| 值 | 页面上写的 | 释义（`VERDICT_WHY`） |
|---|---|---|
| `ok` | **都验到了** | 脚本声明覆盖的场景，读下来都真在验那件事 |
| `risky` | **部分没验到** | 一部分场景脚本认领了、但没真验：断言太松，或在这个环境里压根没跑 |
| `bad` | **多数没验到** | 认领的那几条主要场景，多数没真验 ——「已覆盖」这一栏当不了数 |

这三个短语里没有一个字需要读的人再解释一遍。**判断一个结论词行不行，
不是问"好不好听"，是问"读的人还需不需要再问一句『这是什么意思』"** ——
需要，就是没写完。释义照旧贴着走，凡是摆结论的地方（页面、导出的 Markdown、MCP）
都摆一份，别指望人去别处查一个词什么意思。

「我是怎么看的」那三行也不是装饰 —— 别人第一次看到这份结论，第一个念头是
"你凭什么这么说"。方法（读清单要验什么 → 读脚本实际验什么 → 问**这条断言能不能失败**）
和**边界**（一份脚本都没真跑）都得摆出来。但它每个域都是同一段话，
24 个域连着看就是同一段读 24 遍，所以**默认折起来**，只留一行"读了 N 份脚本、
M 条场景，一份都没真跑"，后面挂个「怎么看的？」。**唯一不折的是"没读全"那句警告** ——
那句是结论的有效范围，折起来等于没说。

`brief.solid`（撑得住的部分）也是必填：整页只有坏消息，读的人会当成"这个域全不能用"，
下次就不看了。

### 给人看那页每条只给一句话，且每栏最多露 3 条

上一版「给人看」那页渲染的是 `problem` —— 那段是写给动手改脚本的人的，
一条三四行，一个域十几行。24 个域连着看，一个域都要看半天。

现在每条只渲染 `oneLine`（模型必填，≤20 字，不许出现路径 / 表名 / 字段名 /
函数名 / 状态码），每个「谁动手」分栏最多露 3 条，多的写「还有 N 条 —— 切到
「给 AI / 整改」那一页」。**人在这一页只做一个决定：这个域要不要停下来处理。**
逐条整改是隔壁那页的事，两页读者不同，别互相迁就。

### 「读不完」不是硬约束，是我自己设的额度 —— 量一遍，然后分批读完

第一版的额度是拍脑袋定的，注释还写着"QA 脚本中位数 2.4KB，6K 能装下九成"。
2026-08-27 真去量了一遍 uag-qa 的 109 份脚本：

| | 实测 |
|---|---|
| 中位数 | **5.3 KB** |
| p90 | 11.6 KB |
| 最大单份 | 17.7 KB |
| MCP 域（最大的域） | 47 份脚本 / **409 KB** / 75 条场景 |
| 其余 23 个域 | 每域 ≤45 KB，一次调用装得下 |

也就是说 `MAX_SCRIPT_BYTES = 6_000` **把全仓一半的脚本截了**，而 prompt 里写着
"截断的不下结论" —— 于是「没发现问题」里有一大块其实是「没读到」。
**这两件事在页面上长得一模一样。**

改法不是把额度调大了事（MCP 域 409 KB 谁调都装不进一轮对话），而是**分批**：

- `MAX_SCRIPT_BYTES = 18_000` —— 实测最大 17.7 KB，这个数下全仓 0 份被截；
- `BATCH_SCRIPT_BYTES = 90_000` —— **一次模型调用**带多少正文，超了切下一批；
- `split_batches()` 切的是**调用**不是内容：每一份脚本都会被读到，只是不在同一次对话里。
  每批都拿到**完整的场景清单**（不然模型不知道这批脚本认领的场景在整个域里什么位置），
  并明写"只对下面出现的脚本下结论，别对没给你的说『没验到』"；
- 各批并发发出去（串行会把一个域的耗时乘上批数，人在页面上等的就是这个数），
  但 `BATCH_CONCURRENCY = 3` 限流 —— 5 批一起打网关，实测 5 个 429 全降级到 CLI 通道；
- **一批挂掉不许把别的批读到的东西一起扔了**（`gather(return_exceptions=True)`）。
  实测撞过：5 批里 3 批同吃 429，`gather` 抛出去，已读完的两批结果跟着没了，
  页面只剩一句「评审失败」，整个域重跑十分钟。现在是全挂才算失败，部分挂了
  留下能读到的，并把批次号记进 `coverage.batchesFailed` —— 页面用红字露在外面，
  导出的 Markdown 也写一句「第 2、4 批没读成，下面『没抓到问题』的部分不包括它们」。
  **少读一批 = 少读十几份脚本，而「少读了」和「没问题」在页面上长得一模一样**，
  这是这套评审最要防的一件事，不许静默；
- `merge_results()` 合并：结论取**最坏**的那一批（5 批里 1 批「多数没验到」，
  整个域就不能当数；平均一下会把最要命的那批稀释掉），按条去重、按严重度排；
- 最后一次**收口调用**只看合并后的结论、**看不到脚本正文** —— 它没法编出一段
  仓库里没有的代码。收口挂了不算评审失败，人话那段退回拼接版。
  收口的 payload 里**代码先把三堆的条数算好喂进去**，并禁止模型自己数：
  实测它写过「清单要商量：17 处」而页面那一栏底下只列 1 条（17 数的是
  `catalogGaps`，1 数的是 `scriptGaps` 里 `blame=catalog` 的）。两个数都对，
  摆在同一屏里就是打架，读的人只会得出「这页的数不能信」。

页面和导出都要说清这次是哪种：分批时写「分 N 批读完再合并 —— **47 份全读了**，
不是抽了几份」；真有没读到的（批数封顶 `MAX_BATCHES`、或有批次没读成）照旧挂 ⚠。

### 给人看那页：`catalogGaps` 也得有个归处

`catalogGaps`（清单缺场景 / 口径名不副实）此前只在「给 AI / 整改」那页露过，
于是人看的那页出现过一句「清单要商量：17 处」，底下那一栏只列 1 条。
现在三栏按「谁动手」收全：`blame=script` / `blame=env` / `blame=catalog` **加上**
`catalogGaps`。**分栏的前提是每条都有归处** —— 有一张表没进任何一栏，
它就等于不存在，而摘要里又会引用它的条数。

### QA 那边怎么拿到这份结论：**只能是他来拉，不是我们推**

这是 §1 那条只读规则的直接后果，不是懒。往他仓库里写一个
`review-MCP.md`，`check-coverage.sh` 会把它当成一份脚本去解析（那个脚本的判据来源
就是仓库里的文件），于是他的门禁红在一个查不到原因的地方 —— **平台单方面改坏了
别人的门禁**，比不给结论坏得多。

所以三条路都是「拿」：

| 通道 | 给谁 | 形态 |
|---|---|---|
| `GET /reviews/{id}/export?format=md` | 脚本 / CI / 想自己存档的人 | 一份完整 Markdown（`to_markdown()`），带 filename |
| 抽屉里「复制 Markdown」/「存成 .md」 | 人 | 同一份东西，粘进 IM 或提到他仓库的 issue 里 |
| MCP 工具 `lum_get_qa_review` | **QA 那边跑的 Claude Code** | `format=md` 拿同一份；`format=json` 拿结构化的 `scriptGaps`，照 `evidence` 逐条改 |

`format=json` 那条是主用法：QA 侧的 CC 拿到 `{path, problem, evidence, fix}`，
`grep` 定位、改脚本、自己跑一遍。平台全程没碰他的仓库。

导出的 Markdown 里**必须带齐三样**，否则两周后这份结论没法判断还算不算数：
评的是哪个 commit、在哪个环境上评的、哪几份脚本进了模型（截断的标「已截断」）。
`tests/test_qa_catalog_review.py::TestMarkdown` 钉住了这三样 + 只读声明。

MCP 侧的两道配套（都是封样测试逼出来的，不是想起来才加的）：

- `review_id` 进了 `_OWNER_SQL`（反查 `qa_catalog_reviews.project_id`）。这条尤其不能漏——
  结论里逐条列着**别人 QA 仓**的脚本路径和判据原文，A 项目的 Key 拿 B 项目的
  reviewId 读出来，泄的是第三方仓库的内容。
- 单开一个 `qareview` 档位，只有 `lum_list_projects` + `lum_get_qa_review`。
  读结论的人跟写用例的人不是同一拨，给他全链路那一档等于把整个用例库也交出去。

### 边界

- 只读、不回写，见上。**结论怎么流出去也只有「拉」这一种**，见上一节。
- 结论是**建议**，清单是别人维护的 —— 页面上「清单本身漏了什么」那一节写明了这一点。
  导出的 Markdown 页脚也写着这句：改不改由仓库主人定。
- 正文被截断的脚本，prompt 里标了「已截断」并要求"拿不准就别列"。
