# CC ↔ 平台闭环：需求与落地方案

> 2026-08-08 定稿。来源：三轮 party-mode 讨论（Winston 架构 / Murat 测试架构 / Amelia 开发 / Sally UX / John 产品）+ 代码库实测核查。
> 这份文档的作用是**防走偏**——后面每做一件事，回来对一遍"边界规则"和"红线"。

---

## 0. 一句话边界规则（所有争议以此仲裁）

> **平台是被测系统的「事实库 + 可重复执行器」；外部 Claude Code 是「一次性的智能」。**
> **需要开浏览器 / 试错 / 看现场才能得出的结论，在平台外产生；需要跨时间、跨人、跨环境重放的东西，在平台内存着。**

判定方法：对每个功能问一句——它的产出是「一次性推理结果」还是「可重放的事实」？

| | CC 端（开发者本机） | 平台 |
|---|---|---|
| 写 / 改脚本 | **唯一写入方** | 只读 + 版本存档 + 回滚 |
| 首次跑通验证 | **必须在这里** | 不做 |
| 回归重放 | 不做 | **唯一执行者** |
| debug / 试错 | **唯一** | 不做 |
| 失败**分类**（是什么） | 不做 | **平台确定性规则** |
| 失败**归因**（为什么） | **CC** | 只存，不判 |
| 状态写入（通过率、维度状态） | 不能 | **唯一写入口** |

### 三条红线

1. **平台侧 AI 生成能力归零，不是降权。** 不保留"平台也能生成，只是弱一点"的分支——否则两年后是两套生成路径、两套 prompt、两套失败模式。
2. **回推的是脚本，不是结论。** CC 说"我本地跑通了"只作为参考；`ui_status` / `api_status` 升到 `executable`，必须以**平台执行器至少成功跑过一次**为准。
3. **CC 的归因不改任何状态。** 它进"待确认"通道。人在平台上的确认动作，是状态的**唯一**写入口。

---

## 1. 目标流程

```
人：「服务管理模块 ui_status=未开始 的，三件套都做了；订阅管理只做步骤+接口」
                            ↓
CC：建立批次意图 → 逐条推进 → 每完成一个维度立刻回推
                            ↓
平台：执行（记账）→ 出报告 → 失败自动分类 + 打包证据
                            ↓
CC：领失败 → 读证据（截图/HAR/步骤轨迹/脚本）→ 结构化归因回填
                            ↓
人：在平台上看 CC 的归因 → 确认 or 推翻 → 分流处置
                            ↓
      script_bug → CC 改脚本回推 → 平台复跑（回到执行）
      system_bug → CC 起草 issue，人按发送（gh issue create，平台只存 issue_url）
      case_expired → 人改用例
      dependency → 补共享资源
```

**不做全自动无人闭环。** 没有调度、没有 CI，也不该现在建。形态是"**一次人工触发，一整轮自动跑完，停在人工闸口前**"。

---

## 2. 关键设计决策（含被否决的方案）

### 2.1 "一次性生成三件套" → 意图一次下达，产物逐个回推

**否决**：把 N 条用例 × 3 种产物做成一个原子批处理。
**理由**：60 个可独立失败的动作捆成一根绳子，断在哪平台上看不出来，只能整批重来。

**采纳**：
- 人的意图落成用例上的一个字段 `target_level`（`spec` / `spec+api` / `full`），**不新建批次表**——意图最终就是"每条要做到什么程度"，存在用例上它才是持久事实而不是一句聊天记录
- CC 每完成一个维度立刻回推一次，对应的维度状态（`manual_status` / `api_status` / `ui_status`）随之推进
- **断点续跑的判据就是这三个现成字段**：CC 续跑 = "把 `ui_status != executable` 的挑出来接着干"

### 2.2 三件套同源生成的质量代价（Murat 的反对意见，已采纳为约束）

传统流程里，步骤用例和自动化脚本是**两次独立的理解**。写脚本时发现和用例对不上而"卡住"，这个卡住本身就是缺陷检出。同源生成会让三份产物**必然互相一致**，而一致会被误读成"验证过了"——**一致性冒充正确性**。

典型失效：CC 把"创建成功"理解成"返回 200"，于是步骤写"预期：创建成功"、接口断言 `status==200`、UI 断言 toast 含"成功"。三层全绿。但真实业务是异步创建，200 只代表受理。**三件套同源，没有任何一层能发现。**

**采纳的分档策略**：

| 优先级 | 产物 | 方式 |
|---|---|---|
| **P0** | 三件套 | **强制两阶段**：先只生成步骤用例 → 人**只看「预期结果」这一列**并确认 → CC 再据此生成接口 + UI |
| P1 / P2 | 步骤 + 接口 | 一次性，允许 |
| P3 | 步骤 | 一次性，允许 |

P0 的人工介入点**刻意选得极窄**——不让人审全文（会疲劳、会盖章），只看预期结果一列，一屏二三十条五分钟过完。这是用最低人力把丢掉的交叉验证买回来。

平台侧强制：**`priority=P0` 的用例不允许一次性三件套直出，人显式选了也不行。** 质量门禁要能覆盖人的手滑。

### 2.3 失败分析：分层，不分栏

**否决**：平台判一个原因、CC 判一个原因，页面并排两栏让人选。
**理由**：把矛盾转嫁给用户；两栏一旦不一致，用户对两边都失去信任。

**采纳**：
- **平台判「是什么」**（分类）：超时 / 元素找不到 / 断言不匹配 / HTTP 5xx / 脚本异常。grep 错误栈 + HAR 就能出，确定性规则，不用 AI
- **CC 判「为什么」**（归因）：需要翻流量、看截图、读代码才能得出的因果链
- 页面上是**一条记录**：类型标签（平台给）+ 分析正文（CC 给，带署名和 run_id）。没归因过就只有标签——**这天然构成待办队列**

### 2.4 运动员兼裁判问题（CC 分析自己写的脚本）

偏差**确定存在**且方向可预测：系统性低估"脚本自己有问题"（把测试缺陷伪装成产品缺陷）、系统性低估 flaky（CC 看不到历史）。

**不禁止，用三条锁住**：
1. 归因**必须结构化 + 必须自证**：`evidence` 字段必须引用**平台侧证据**的具体位置（哪一步 / 哪条断言 / error_summary 哪句 / 第几张截图）。**没有指向平台侧记录的 evidence，直接拒收。**
2. 归因**不改状态**（红线 3）
3. 上线反向指标：`CC归因 vs 人确认` 一致率，按 `failure_type` 分桶。`product_defect` 桶推翻率 > 30% → 触发人工复核 CC 的归因能力。**这是目前平台完全没有的东西：对 AI 判断本身的质量度量。**

### 2.5 防退化锁（Murat + Winston 独立提出的同一件事）

**这套架构最大也最隐蔽的失败模式：CC 会"改得太好"。**

给它"让测试变绿"的目标，它一定能做到——加 `waitForTimeout(5000)`、放宽断言、`try/catch` 吞异常、条件 `skip`。每一条都能变绿，每一条都在销毁这条测试存在的理由。**这个失败不会以"报错"出现，它会以"一切正常"出现。**

**锁**：
- 每次回推记录**断言指纹**（条数 + matcher 类型分布 + 强度评分）
- 修复版本的指纹**不允许弱于上一版**；变弱 → 拒收，除非 CC 显式声明"上一版断言错误"并给理由，转人工队列
- 同一脚本**连续修复 3 次仍未通过 → 强制转人工**

⚠️ **Amelia 的技术否决（已采纳）**：断言"强度"**做不到可靠硬拦截**。Playwright 断言形态太多（`expect().to_be_visible()` / `assert` / `wait_for_selector` 都算；一条 `to_contain_text` 拆成两条数量还涨了但强度降了）。误拦会逼 CC 想办法绕（拆断言凑数），比不拦更糟。

**落地版**：
- **硬拦截只保留一条：新版本断言总数为 0**（100% 可判，且是最常见的作弊路径）
- 其余降级为**软警告 + 记账**：存 `assertion_profile`，回推响应返回 `warnings: ["强断言 to_have_text 3→1，弱断言 to_be_visible 1→3，疑似削弱"]`

### 2.6 批量入库的四道闸（全部在回推入口）

批量生成会淹库，两种淹法，第二种更难受：
- 明面的：300 条重复语义、断言宽松的垃圾——好发现
- **暗面的：每条单看都合理，合起来覆盖度极度倾斜**。20 条都在测"创建服务"的参数组合，零条测删除的级联影响。**总量涨了，风险覆盖没涨**，而通过率、用例数全面美化

已经踩过一次（"P0 占比超标 / 模糊词 / module 为空"），那次是入库后发现；批量化之后入库后发现 = 人肉从 300 条里捞。

| # | 闸 | 动作 |
|---|---|---|
| 1 | **语义去重** | 与同 module 已有用例比相似度，超阈值**拒**，返回"疑似重复于 XXX" |
| 2 | **断言强度** | 只断言 status code / 只断言"不为空" / 只断言 toast 文案 → 标 `weak_assertion`，**可入库但永不能升到 executable** |
| 3 | **P0 配额** | 单次批量中 P0 占比硬上限 **15%**，超了整批打回让 CC 重新分级 |
| 4 | **覆盖倾斜** | 算这批打在哪几个 module / 哪几类操作（增删改查 / 异常 / 权限 / 并发），某格过高或「删除」「权限」为零 → **告警 + 人确认** |

1~3 平台自动拒，4 告警。**全部作用在 MCP 回推入口——那是唯一入口，这是架构上最大的便宜，一定要占。**

### 2.7 调试执行 vs 记账执行

`script_runs` 加 `run_mode`（`regression` / `debug`）。通过率、报告口径只算 `regression`。
- `tb_run_ui_script`（单条，语义就是"聚焦调试用"）→ 默认 `debug`
- `tb_run_ui_scripts_batch`（批量回归）→ 默认 `regression`

跟工具现有语义正好对齐，不用改调用方。

### 2.8 明确不做

| 不做 | 理由 |
|---|---|
| 平台侧 AI 自愈 / 自动改脚本 | 几十次实测失败已证；自愈 = 带失败上下文的生成，难度只高不低 |
| `repair_queue` 表 + `tb_claim_repairs` + 自愈次数计数器 | 为"多 CC 实例并发领任务、无人值守失控"这个不存在的场景做工程。**现在是一个人，人就是护栏。** 待修清单 = 执行历史上一个筛选条件 |
| 定时调度（cron / celery / APScheduler） | 会引入一个需要长驻、需要监控、会静默死掉的组件；已有两个长驻进程的维护成本。触发用 CC 的 `/loop` 或本地 crontab |
| 缺陷系统对接（Jira / 禅道） | CC 端有 `gh` 和文件系统，issue 在那边提，平台只存 `issue_url` 回链。**平台一行代码不用写** |
| 「病历」概念 + `case_file_events` 独立页签 | 三条时间线（病历 / 执行历史 / 生成档案）说的是同一件事，只需要一条 |
| `case_expired` 自动分诊 | 需要知道"预期该是什么"，HAR 里没有这个信息。并入 `unknown`，交给 CC 用视觉判 |
| healing_context「≥6 条压缩成摘要」 | 那是外部 agent 上下文有限的实现细节。**平台永远不该存外部智能的中间态。** |

---

## 3. 代码库现状核查（2026-08-08 实测，不是推测）

### 已通
- `tb_sync_ui_script` 回推 → 页签渲染 → 平台执行 `passed 5.3s`；写死地址/凭据被硬拦截
- `tb_sync_orchestrated_scenario` 接口场景回推，硬拦截悬空 `${x}`
- `tb_create_case` / `tb_upsert_scenario_variables` / `tb_get_sync_spec` / `tb_list_global_data`
- `POST /plans/{id}/execute` 批量执行 + 报告
- `scripts` 表版本 + `activate` 回滚（**零 AI 风险的自愈，能力已有，UI 上不显眼**）
- 项目级共享资源 `exists_check` 执行前探测注入

### 坏的 / 缺的

| 项 | 现状 |
|---|---|
| **执行历史 type 参数** | 前端传用例 `type`（`api`/`e2e`），后端要脚本类型（`ui`/`api`）。TC-XMGL-00001 库里 2 条 UI 记录，页面显示 0 条。**e2e 类型用例永远查不到** |
| **`captured_requests`（HAR）** | 平台执行 UI 脚本**不录网络流量**。`tea_capture` patch 的是 httpx，Playwright 浏览器流量不经过它。`script_runs` 也没有这个列 |
| **`failure_type`** | **从来没落过库**。只存在于 `step_generator.py`（已下线的平台生成管道）。前端那几个 script_bug/system_bug 标签，生成入口下线后已永远不会出现 |
| **`tb_get_ui_script_result`** | 只返回 `screenshots_count`（数字），**CC 看不到截图** |
| **MCP 测试计划工具** | **一个都没有**。`tb_get_report_summary` / `tb_get_failed_scenarios` 要 `plan_id`，但没有 `tb_list_plans` → **这两个工具实际上是死的** |
| **`tb_generate_ui_script`** | 平台侧生成工具**还挂在 MCP 上**（页面入口已删）。留着就是留一条暗路，违反红线 1 |
| **`tb_list_cases`** | 不支持按维度状态筛（REST 支持 `uiStatus=xxx`，MCP 没透出）→ CC 无法断点续跑 |
| **需求溯源** | 只渲染裸编号 `R3`。`requirement_points` 有 107 条，含标题 + `quote_text` + 字符偏移锚点 |
| **`case_file_events`** | 全平台 0 行，无任何写入方，能力总览却标"已完成" |
| **AI 能力注册表** | 14 项中 **4 项已死**：`ui-script` / `ui-script-repair` / `pytest-script` / `toolbox-regex`（前端入口已删或从无调用方）。**`ui_script` 整个类别现在是空的** |
| **MCP Key 授权范围** | 后端 `PATCH /api/mcp-keys/{key_id}` 早就支持改且立即生效；前端「工具范围」按钮在**连接管理**页签，用户在**工具列表**页签只看到「按这个范围建 Key」→ 以为只能新建 |
| **AI 配置页两个数字打架** | 上半表格显示 `claude-sonnet-4-6`（历史配置值），下半「实际生效」显示 `claude-sonnet-5`。**`/api/ai-capabilities/models` 实测第一个就是 `claude-opus-5`**——"只能选 4.6"是误读，真问题是不知道哪个数字算数 |

---

## 4. Story 清单与排期

### 第一刀 · 让失败看得见（唯一承诺范围）

**主线判据**：造 5 个真实失败，分类器答对 4 个。**答不对就回来改规则，不准往下做。**

| # | Story | 改哪里 | 大小 |
|---|---|---|---|
| **A1** | 修执行历史 type 参数 bug | 前端传脚本类型而非用例类型 | XS |
| **A2** | 摘掉 `tb_generate_ui_script`；从 AI 能力注册表删除 4 个死能力 | `app/mcp/__init__.py`、`app/services/ai_capabilities.py` | XS |
| **A3** | **HAR 录制 + 落库** | `pw_conftest.py` 的 `browser_context_args` 加 `record_har_path`；复用 `cli_agent._parse_har`（已滤静态资源）；`script_runs` 加 `captured_requests jsonb` | S |
| **A4** | `script_runs` 加 `failure_type` + 平台侧确定性分类 | 新文件 `app/services/failure_triage.py`。**失败时刻前 5s 时间窗 + 排非本域**。输出 `dependency` / `system_bug` / `script_bug` / `unknown` 四类。**必须有单测，用真实失败样本做 fixture** | M |
| **A5** | `tb_get_ui_script_result` 返回**截图本身** + HAR 摘要 + 失败步骤上下文 | `app/mcp/tools/ui_scripts.py` | S |
| **A6** | 执行模式分离 `run_mode` | `script_runs` 加列；`/run` 收 `mode`；通过率口径加 filter；`tb_run_ui_script`→debug，`batch`→regression | S |

> **为什么 A3~A5 排在计划工具之前**：Winston 原排序是先补计划工具，Murat 反对——"给 CC 看到截图和失败上下文，比做计划工具更急"。采纳 Murat，理由用 Winston 自己的话：证据这一步"**人自己看失败也变轻松了，不依赖 CC 也回本**"，是唯一一个不依赖后续步骤就能独立兑现价值的。计划工具做完只是让 CC 能按按钮——人本来就能按。

### 第二刀 · 让 CC 能分析、人能确认

| # | Story | 大小 |
|---|---|---|
| B1 | MCP 计划四件套：`tb_list_plans` / `tb_create_plan` / `tb_run_plan` / `tb_list_reports`（救活现有两个死工具） | S |
| B2 | `script_runs` 加 `cc_analysis`（结构化：`failure_type` + `evidence` + `confidence` + `proposed_fix_target` + 署名 + `run_id`）；**无 evidence 拒收** | S |
| B3 | `tb_submit_analysis` MCP 工具 | S |
| B4 | 平台确认入口：`confirmed_cause` + `confirmed_note`（必填），**机器永不覆盖**；页面一个下拉 + 一句理由 | S |
| B5 | 回推证据门禁：payload 带 `evidence{duration_ms, assertion_count}`，缺失拒收；**断言数为 0 硬拦截**；`assertion_profile` 软警告 | S |
| B6 | `CC归因 vs 人确认` 一致率指标（按 failure_type 分桶） | S |

### 第三刀 · 批量推进 + 入库闸门

| # | Story | 大小 |
|---|---|---|
| C1 | 用例加 `target_level`（spec / spec+api / full） | XS |
| C2 | `tb_list_cases` 支持按维度状态筛选 → CC 断点续跑 | XS |
| C3 | 入库四道闸（语义去重 / 断言强度 / P0 配额 15% / 覆盖倾斜告警） | M |
| C4 | P0 强制两阶段（人只确认「预期结果」一列） | M |
| C5 | `/tb-regression` skill（**走 `tb_push_skill` 存 DB，不要放 `app/skills/preset/`**，CLAUDE.md 明令） | M |

### 独立线 · UX 清晰度（Sally 方案）

| # | Story | 大小 |
|---|---|---|
| **U1** | **AI 配置页第一层卡片**：大号模型 ID + 网关地址 + 连接状态 + "这一个模型负责全部 N 项能力"。**首屏只出现一个模型号** | S |
| U2 | 「换模型」弹窗：**测通之前「保存并启用」禁用**；代理状态明写；"网关实时返回的列表，不是写死的"；失败给可执行建议不给状态码；卡片右下角"上次变更…[退回]" | S |
| U3 | 「哪些地方用 AI」改成三列表（**干什么 / 用户从哪进 / 用哪个模型**），删掉标签云 | S |
| U4 | Provider 配置行**不再存模型，只存连接**（服务商+地址+凭证+健康）；模型是能力层属性 → **结构上不可能再出现两个模型号** | M |
| U5 | MCP 工具列表改成**流程视图**（这个活分几步 / 每步在干嘛 / 背后哪几个工具默认折叠）；**删掉参数名列表**；「只看用到的」默认勾上 | M |
| U6 | 「应用范围到已有 Key」入口 + 每个 Key 显示 diff（`▸ 改成 19` / `✓ 已一致`）+ 紧贴按钮那句"**改完立即生效，Key 本身不变，不用重配 CC**" | S |

> Sally 的排序建议：**如果只能做一件事，做 U1**。其他两个抱怨是"用起来累"，AI 配置那个是"我不信你"——**信任先修**。

### 顺手做（各 XS，不占刀）
- 需求溯源渲染真数据（标题 + 原文引用 + 锚点跳转），107 条现成
- 能力总览把假"已完成"改成真状态
- `scripts` 版本回滚提到显眼位置（零 AI 风险的自愈，能力已付过钱）

---

## 5. 验收标准（可判定，不含"用户满意"）

### 第一刀
1. 执行历史对有 N 条记录的用例显示 N 条（N>0），type 切换后数量正确
2. 回推**断言数为 0** 的脚本 → 明确拒绝，**查库确认无记录**
3. 回推合法脚本（带本地通过证据）→ 入库 → 平台以 `regression` 模式执行 → 执行历史出现 1 条新记录
4. 手动改坏被测系统一处（改掉某按钮 id）→ 分类显示 `script_bug` **而不是 `unknown`**
5. 手动让某接口返回 500 → 分类显示 `system_bug`
6. 4/5 两次执行中出现 favicon 等静态资源 404 时，**分类结论不变**（验证时间窗 + 静态资源过滤生效）
7. `tb_get_ui_script_result` 返回的截图，CC 能实际看到内容（不是数量）

**成败判据：造 5 个失败，分类器答对 4 个。**

### 第二刀
1. CC 提交无 `evidence` 的归因 → 被拒收
2. CC 归因写入后，用例的 `ui_status` / 通过率 / 报告结论**一个字节都没变**
3. 人在页面确认后，状态才变
4. `tb_list_plans` 能拿到 plan_id，`tb_get_report_summary` 用该 id 能返回数据

### 第三刀
1. CC 中断后重跑，只处理 `ui_status != executable` 的用例，已完成的不重做
2. 单批含 >15% P0 的回推被整批打回
3. `priority=P0` 的用例走一次性三件套 → 被平台拒绝，提示走两阶段

---

## 6. 延后 / 需要真实数据才能定的

| 项 | 为什么延后 | 解锁条件 |
|---|---|---|
| flaky 隔离（`quarantined` 状态、14 天到期飘红） | 现在脚本数是个位数，隔离机制服务的是想象中的规模 | 真有 30 条脚本在跑、真被 flaky 咬过一次 |
| flaky 判定阈值（3 轮内 2 次翻转） | Murat 明说"**这个数字没有数据支撑，是经验值**" | 跑满一个月，拿真实 executions 的翻转分布校准 |
| MCP 工具按动词分层重整（37 → 15 以内） | 不紧急但会持续恶化 | 趁 37 还不是 80 时做 |
| P0 强制两阶段是否必要 | Murat："若实测 CC 的 P0 三件套一致性错误率 < 5%，我第一个撤回" | 攒够 P0 三件套的实测数据 |
| 平台自动分类的必要性 | 有了 CC 归因，平台规则初判从"必需"降为"锦上添花" | 若 CC 归因质量够，A4 可简化 |

---

## 7. 附：这份方案怎么来的

三轮 party-mode，主要分歧与裁决：

| 分歧 | 各方 | 裁决 |
|---|---|---|
| 病历怎么办 | John 抬成一等公民新建写入方 / Murat + Winston 改成 union 聚合 / Sally 直接删 | **删概念**。Winston 后来改投 Sally |
| 先做证据还是先做计划工具 | Winston 计划优先 / Murat 证据优先 | **证据优先**，用 Winston 自己的"不依赖 CC 也回本"当理由 |
| 断言削弱要不要硬拦截 | Murat 要求与"写死拦截"同级 / Amelia 技术上判定做不到 | **Amelia 胜**。降级软警告，硬拦截只留"断言数为 0" |
| repair_queue 要不要建 | Winston + Murat + Amelia 都设计了 / John 全砍 | **John 胜**。一个人的场景不需要状态机 |
| `case_expired` 自动分诊 | Winston 给了规则 / Amelia 判定无可判据 | **Amelia 胜**，并入 `unknown` |

**共四票一致、无异议的**：执行历史 bug 是 P0；需求溯源接真数据；**平台侧不做 AI 自愈**；能力总览假"已完成"要改。
